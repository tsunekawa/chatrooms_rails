<h1>Messages</h1>

<ul id="chat" class="message-list">
  <%= render @messages %>  
</ul>

<%= form_for(Message.new, remote: true) do |f| %>
 <%= f.text_field :content %>
 <%= f.submit "send" %>
<% end %>

<%= subscribe_to "/messages/new" %>
